var t = require('./')(require('./test/keys.json'));

function pepito(words){
t.filter({track: words}, function(res){
  res.on('tweet', function(data){console.log(data.text);})
  setTimeout(function(){res.end();pepito(['messi']);}, 10000);
});
}

pepito(['love']);


